Generate a mapping of placeholder to entity, given the following two maps.
[MAP#1] associates an placeholder with an entity, and [MAP#2] associates an entity with a placeholder.

Join the maps based on the entities to create a map of placeholders to entities, e.g., "{{"placeholder": "entity"}}". In other words, this is the same format as [MAP#1], but with any new entries added from [MAP#2].

Output the final mapping as a JSON object with the placeholder as the key and the entity as the value.

Remember that the placeholders in [MAP#1] are unique and each refer to at most one entity in [MAP#2]. 

Every single entity in [MAP#1] should be included in your output; your job is to see if any new entries are present in [MAP#2]. NEVER omit entries from [MAP#1] in your output.

Remember that entities may have human names or nicknames and so might vary (nicknames, abbreviations, etc.). This means multiple variants of a name might map to the same alias, e.g., "Officer Smith" and "Officer John Smith" mapping to the same alias "Officer 1".

Use [NARRATIVE] for additional context to determine which names in [MAP#2] refer to the same person.

Return ONLY the final output in JSON. Do NOT include any other text in the response. In particular, do NOT include any header, e.g. do not include "JSON" at the top of your response. Consolidate any duplicate keys. There should be no duplicate keys in your output. 

For example, given the following inputs:
```[MAP#1]
{{
  "accused 1": "John Smith",
  "victim 1": "Jane Doe"
}}

[MAP#2]
{{
  "John Smith": "Accused 1",
  "Johnny": "Accused 1",
  "Jane Doe": "Victim 1",
  "Ms. Doe": "Victim 1",
  "123 Main Street": "Address 1 on Street 1"
}}

[NARRATIVE]
At TPO, I responded to a call from Jane Doe at 123 Main Street. The accused, John Smith, was arrested at the scene. When I left, Ms. Smith called out, "Johnny, don't leave me!"```

The corresponding output would be:
```{{"accused 1": "John 'Johnny' Smith", "victim 1": "Jane Doe", "address 1": "123 Main Street", "street 1": "Main Street"}}```

Note again that the example output does not include a header.
Your output should also not include a header.