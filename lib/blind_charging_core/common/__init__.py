# Import pylibmagic for the side-effects. This also has to be
# imported *before* `magic` is used. So import her here, trying
# to make sure it doesn't get auto-sorted out of order under magic.
import pylibmagic  # noqa
